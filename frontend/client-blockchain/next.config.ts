import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['192.168.1.3', "https://cleansingly-timorous-margarett.ngrok-free.dev"],
};

export default nextConfig;
